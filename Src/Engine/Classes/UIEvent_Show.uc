/** ScreenObject가 감춰졌다 다시 보일때 불리는 UIEvent
 *
 * @note: native because C++ code activates this event
 */
class UIEvent_Show extends UIEvent
	native(inherit);

DefaultProperties
{
	ObjName="Show"

	ObjPosX=160
	ObjPosY=140
}
